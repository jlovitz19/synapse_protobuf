syntax = "proto3";

import "header.proto";
import "nanopb.proto";

message Actuators {
	Header header = 1;
	repeated double position = 2 [(nanopb).max_count = 16];
	repeated double velocity = 3 [(nanopb).max_count = 16];
	repeated double normalized = 4 [(nanopb).max_count = 16];
}
